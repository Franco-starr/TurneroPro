# Blueprint de Render para TurneroPro.
#
# Cómo usarlo: subí este archivo a GitHub en la raíz del repo y, al crear el
# Web Service, elegí "Blueprint" (o pulsá "New > Blueprint") y Render lee la
# config. Igual lo importante es el Dockerfile: Render detecta runtime Docker.
services:
  - type: web
    name: turneropro
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    healthCheckPath: /up
    envVars:
      - key: APP_NAME
        value: TurneroPro
      - key: APP_ENV
        value: production
      - key: APP_LOCALE
        value: es
      - key: APP_DEBUG
        value: "false"
      - key: APP_URL
        value: https://turneropro.onrender.com
      # APP_KEY no se define acá: se setea a mano en el dashboard (php artisan key:generate --show).
      # generateValue generar valores que Laravel puede rechazar; deploy.sh autovalida y regenera.
      - key: DB_CONNECTION
        value: sqlite
      - key: SESSION_SECURE_COOKIE
        value: "true"
      - key: MAIL_MAILER
        value: log